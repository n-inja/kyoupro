#include<iostream>
#include<fstream>
#include<stdio.h>
#include<string>
#include<vector>
#include<map>
#include<math.h>
#include<algorithm>
#include<iomanip>
#include<set>

using namespace std;

int w, h;
vector<long long> p, q;

int main() {
  p.resize(w);
  q.resize(h);
  for(int i = 0; i < w; i++) cin >> p[i];
  for(int i = 0; i < h; i++) cin >> q[i];


  return 0;
}
