#include <bits/stdc++.h>

using namespace std;

using ll = long long;
using vi = vector<int>;
using vll = vector<ll>;
using vvi = vector<vector<int>>;
using vvl = vector<vector<ll>>;

void solve() {
  ll a, b, c;
  cin >> a >> b >> c;
  cout << (a + b + c) / 2 << endl;

}

int main() {
  cin.tie(nullptr);
  ios::sync_with_stdio(false);
  int t;
  cin >> t;
  while (t--)solve();
  return 0;
}
